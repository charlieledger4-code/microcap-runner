# Exact runtime used by the 2026-08-29 GitHub Actions frozen-model build.
# Keep separate from general research dependencies so serialized sklearn models
# are not silently loaded under a materially different environment.
numpy==2.4.6
scipy==1.17.1
pandas==3.0.5
scikit-learn==1.9.0
joblib==1.5.3
